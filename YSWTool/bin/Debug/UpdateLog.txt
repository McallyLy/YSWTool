V0.9.1
2017年5月25日23:33:12
1.新增按钮“参数类型”，对应生成参数类型的SQL代码。
2.调整了配置页对于表结构，参数，参数类型等页签的设定。为了方便易懂，页签设定时从1数起。系统自动减一。
3.因为界面问题需要优化，但是目前暂时想不到好的优化。所以版本暂不升为1.0。
4.换了一个稍微好看了一点点的logo。

V0.9
2017年4月10日00:48:49
1.新增了“新增”列，用以在生成SQL代码时，可以选择指定字段不做新操作。
2.调整了SQL代码的生成格式，加入了自动换行设定，让其可以适应小屏幕的显示。

V0.8
2017年4月8日02:16:34
1.调整了打开表的按钮，移除了一些必定不会修改的项的编辑勾选。
2.因为EMO系统的特殊性，在创建modelService时，将其区分开来。

V0.7
2017年4月1日
1.添加系统编号，表和参数表页签的设定，并添加了多系统配置切换。

V0.6
2017年3月16日
1.稍微调整了界面。将整个界面稍微调大一点。原本打算表逻辑和参数的操作各自弹窗的，但是感觉有点复杂化了，经宿友提醒，将逻辑和参数的代码用TabControl区分在不同的页签上即可。

V0.5
2017年3月13日
1.修改了打开参数表的时候，没有读取到父参数流水号的bug.
2.保存配置和只是SQL暂时添加简单的弹窗结果提示。

V0.4
2017年3月5日
1.调整了设置中文件名称的定义。添加选择文件的设定。可以选择所读取的文件。
2.配置界面添加数据库连接语句的记录。
3.对部分按钮做了控制。
4.添加"参数SQL"按钮,生成指定行的参数SQL插入语句。
5.添加"执行SQL"按钮,在点击建表SQL和参数SQL按钮时启用，根据配置中的数据库链接语句执行SQL语句。

V0.3
2017年3月4日
1.调整了界面，将表格和现实内容并行，因为要考虑到屏幕的大小问题。（看了吖良的那个界面后所考虑到的问题）
2.调整了表格的列宽度，以及取消了添加，删除，排序等操作，让表格只显示以及编辑是否可改。
3.修改了日志弹窗的显示方式，读取指定更新日志文件显示。
4.添加"配置"按钮,弹窗配置一些设定。目前是设定文档名，Model命名空间，ModelService命名空间，后续将加入更多的初始化设定。
5.稍微调整了一下界面格式。然后把LOGO也变了！一个应用程序应该拥有属于他的LOGO(虽然现在也是从网上找的)。

V0.2
1.添加“生成Model”按钮，初步生成Model内容。并实现了在桌面生成.cs文件。
2.添加“生成SQL”按钮，初步生成SQL语句。
3.添加“生成ModelService”按钮，初步生成ModelServic的内容，包括添加，更新，获取实体等3个接口。并实现了在桌面生成.cs文件。
4.添加“更新日志”按钮，弹窗显示更新内容，然而并没有什么卵用。因为文字都是写死在控件里面的。

V0.1
1.建立了表格。以显示excel数据。
2.添加起止行输入框。以准确找到数据。
3.添加3个按钮。
  "打开表":根据起止输入框读取指定位置（以后改成自定义？）的excel表格的第二页签也就是数据库的表结构设定页签。
  "打开参数":根据起止输入框读取指定位置（以后改成自定义？）的excel表格的第6页签也就是参数设定页签。
  "清空":清空起止输入框和表格内容。